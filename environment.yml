name: music-catalog-manager
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
    - streamlit
    - pandas
    - numpy
    - matplotlib
    - scikit-learn
    - torch
    - altair
    - watchdog
    # Add any other dependencies here